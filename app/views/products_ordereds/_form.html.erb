<%= form_with(model: products_ordered) do |form| %>
  <% if products_ordered.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(products_ordered.errors.count, "error") %> prohibited this products_ordered from being saved:</h2>

      <ul>
        <% products_ordered.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :qty_ordered, style: "display: block" %>
    <%= form.number_field :qty_ordered %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
